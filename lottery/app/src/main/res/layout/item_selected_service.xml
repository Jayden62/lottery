<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">


    <TextView
            android:layout_marginTop="@dimen/normalMargin"
            android:layout_marginEnd="@dimen/normalMargin"
            app:layout_constraintEnd_toStartOf="@+id/mImageViewRemove"
            app:layout_constraintStart_toStartOf="parent"
            android:gravity="start"
            app:layout_constraintTop_toTopOf="parent"
            android:id="@+id/mTextViewSelectedService"
            android:layout_width="0dp"
            android:layout_height="wrap_content"/>

    <ImageView
            android:clickable="true"
            android:focusable="true"
            android:id="@+id/mImageViewRemove"
            android:layout_marginTop="@dimen/normalMargin"
            android:src="@drawable/bg_deleted_selected_item_service"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

    <View
            android:background="@color/grayColor"
            android:layout_marginTop="@dimen/normalMargin"
            app:layout_constraintTop_toBottomOf="@+id/mTextViewSelectedService"
            android:layout_width="match_parent"
            android:layout_height="1dp"/>

</android.support.constraint.ConstraintLayout>